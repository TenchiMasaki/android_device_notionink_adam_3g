diff --git a/gui/gui.cpp b/gui/gui.cpp
old mode 100644
new mode 100755
index ebd7053..ca114d9
--- a/gui/gui.cpp
+++ b/gui/gui.cpp
@@ -326,13 +326,16 @@ void InputHandler::processHoldAndRepeat()
 
 void InputHandler::doTouchStart()
 {
-	LOGEVENT("TOUCH_START: %d,%d\n", x, y);
-	if (PageManager::NotifyTouch(TOUCH_START, x, y) > 0)
-		state = AS_NO_ACTION;
-	else
-		state = AS_IN_ACTION_AREA;
-	touch_status = TS_TOUCH_AND_HOLD;
-	gettimeofday(&touchStart, NULL);
+	if ((x < 460 && y < 280) || (x > 505 && y > 315) ||
+		(x < 460 && y > 315) || (x > 505 && y < 280)) {
+		LOGEVENT("TOUCH_START: %d,%d\n", x, y);
+		if (PageManager::NotifyTouch(TOUCH_START, x, y) > 0)
+			state = AS_NO_ACTION;
+		else
+			state = AS_IN_ACTION_AREA;
+		touch_status = TS_TOUCH_AND_HOLD;
+		gettimeofday(&touchStart, NULL);
+	}
 }
 
 void InputHandler::process_EV_ABS(input_event& ev)
@@ -340,30 +343,34 @@ void InputHandler::process_EV_ABS(input_event& ev)
 	x = ev.value >> 16;
 	y = ev.value & 0xFFFF;
 
-	if (ev.code == 0)
-	{
-#ifndef TW_USE_KEY_CODE_TOUCH_SYNC
-		if (state == AS_IN_ACTION_AREA)
-		{
-			LOGEVENT("TOUCH_RELEASE: %d,%d\n", x, y);
-			PageManager::NotifyTouch(TOUCH_RELEASE, x, y);
-		}
-		touch_status = TS_NONE;
-#endif
-	}
-	else
-	{
-		if (!touch_status)
+	if ((x < 460 && y < 280) || (x > 505 && y > 315) ||
+		(x < 460 && y > 315) || (x > 505 && y < 280)) {
+
+		if (ev.code == 0)
 		{
-#ifndef TW_USE_KEY_CODE_TOUCH_SYNC
-			doTouchStart();
-#endif
+	#ifndef TW_USE_KEY_CODE_TOUCH_SYNC
+			if (state == AS_IN_ACTION_AREA)
+			{
+				LOGEVENT("TOUCH_RELEASE: %d,%d\n", x, y);
+				PageManager::NotifyTouch(TOUCH_RELEASE, x, y);
+			}
+			touch_status = TS_NONE;
+	#endif
 		}
 		else
 		{
-			if (state == AS_IN_ACTION_AREA)
+			if (!touch_status)
+			{
+	#ifndef TW_USE_KEY_CODE_TOUCH_SYNC
+				doTouchStart();
+	#endif
+			}
+			else
 			{
-				LOGEVENT("TOUCH_DRAG: %d,%d\n", x, y);
+				if (state == AS_IN_ACTION_AREA)
+				{
+					LOGEVENT("TOUCH_DRAG: %d,%d\n", x, y);
+				}
 			}
 		}
 	}
diff --git a/updater/Android.mk b/updater/Android.mk
old mode 100644
new mode 100755
index 2ad56ba..4057d33
--- a/updater/Android.mk
+++ b/updater/Android.mk
@@ -19,7 +19,7 @@ LOCAL_MODULE_TAGS := eng
 
 LOCAL_SRC_FILES := $(updater_src_files)
 
-ifeq ($(TARGET_USERIMAGES_USE_EXT4), true)
+ifeq ($(TARGET_USERIMAGES_USE_EXT4),)
 LOCAL_CFLAGS += -DUSE_EXT4
 LOCAL_CFLAGS += -Wno-unused-parameter
 LOCAL_C_INCLUDES += system/extras/ext4_utils
