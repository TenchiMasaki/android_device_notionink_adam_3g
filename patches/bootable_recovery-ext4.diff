project bootable/recovery/
diff --git a/Android.mk b/Android.mk
old mode 100644
new mode 100755
index 6d916df..5026831
--- a/Android.mk
+++ b/Android.mk
@@ -33,6 +33,8 @@ TWHTCD_PATH := $(TWRES_PATH)htcd/
 
 TARGET_RECOVERY_GUI := true
 
+LOCAL_CPP_INCLUDES += system/extras/ext4_utils
+
 LOCAL_SRC_FILES := \
     twrp.cpp \
     fixPermissions.cpp \
@@ -108,12 +110,12 @@ ifeq ($(TW_OEM_BUILD),true)
     TW_EXCLUDE_SUPERSU := true
     TW_EXCLUDE_MTP := true
 endif
-ifeq ($(TARGET_USERIMAGES_USE_EXT4), true)
+#ifeq ($(TARGET_USERIMAGES_USE_EXT4), true)
     LOCAL_CFLAGS += -DUSE_EXT4
     LOCAL_C_INCLUDES += system/extras/ext4_utils
     LOCAL_SHARED_LIBRARIES += libext4_utils
     LOCAL_STATIC_LIBRARIES += liblz4-static
-endif
+#endif
 ifneq ($(wildcard external/libselinux/Android.mk),)
     TWHAVE_SELINUX := true
 endif
@@ -126,12 +128,12 @@ ifeq ($(TWHAVE_SELINUX), true)
     LOCAL_C_INCLUDES += external/libselinux/include
     LOCAL_SHARED_LIBRARIES += libselinux
     LOCAL_CFLAGS += -DHAVE_SELINUX -g
-    ifneq ($(TARGET_USERIMAGES_USE_EXT4), true)
+#    ifneq ($(TARGET_USERIMAGES_USE_EXT4), true)
         LOCAL_CFLAGS += -DUSE_EXT4
         LOCAL_C_INCLUDES += system/extras/ext4_utils
         LOCAL_SHARED_LIBRARIES += libext4_utils
         LOCAL_STATIC_LIBRARIES += liblz4-static
-    endif
+#    endif
 endif
 
 # This binary is in the recovery ramdisk, which is otherwise a copy of root.
diff --git a/updater/Android.mk b/updater/Android.mk
old mode 100644
new mode 100755
index 2ad56ba..c5e38e5
--- a/updater/Android.mk
+++ b/updater/Android.mk
@@ -19,7 +19,7 @@ LOCAL_MODULE_TAGS := eng
 
 LOCAL_SRC_FILES := $(updater_src_files)
 
-ifeq ($(TARGET_USERIMAGES_USE_EXT4), true)
+#ifeq ($(TARGET_USERIMAGES_USE_EXT4), true)
 LOCAL_CFLAGS += -DUSE_EXT4
 LOCAL_CFLAGS += -Wno-unused-parameter
 LOCAL_C_INCLUDES += system/extras/ext4_utils
@@ -41,7 +41,7 @@ LOCAL_STATIC_LIBRARIES = \
     libz \
     liblz4-static
 endif
-endif
+#endif
 
 LOCAL_STATIC_LIBRARIES += $(TARGET_RECOVERY_UPDATER_LIBS) $(TARGET_RECOVERY_UPDATER_EXTRA_LIBS)
 LOCAL_STATIC_LIBRARIES += libapplypatch libedify libmtdutils libminzip libz
